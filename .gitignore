/.claude
__pycache__/
*.pyc

# Built images and QEMU artifacts
output/

# Cached source tarballs
mirror/

# Rust build artifacts
cli/target/
crates/target/

# Nix build results
result
result-*

# Direnv
.direnv/

# Doc index cache
.aos-doc-cache.json

# Per-operator ignition config (bakes in an SSH authorized_key; see
# docs/boot/qemu-uefi.md §0 — explicitly "not checked in").
/tests/ignition-test.json

# Dev hub state (aos-registry-hub `serve --dev`): local db + seeded bucket.
/crates/.aos-hub/
