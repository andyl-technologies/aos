//! `crucible` is the CLI entry point for the Crucible control plane.
//!
//! This L4 binary crate will remain a thin client over `crucible-api` and
//! `crucible-session` as specified by RFC-0010 files 23 and 27.

#![forbid(unsafe_code)]

fn main() {}
