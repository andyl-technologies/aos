//! Red placeholder for `gate:layer0-determinism`.

#![forbid(unsafe_code)]

#[test]
#[ignore = "T-HARN-5 implements gate:layer0-determinism"]
fn gate_layer0_determinism_is_mapped() {
    panic!("gate:layer0-determinism implementation is pending T-HARN-5");
}
