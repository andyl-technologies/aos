//! Arguments for `aos prefetch` — compute source hashes in parallel.
//!
//! Prefetch args are inline in `Commands::Prefetch` — no separate struct
//! needed. The implementation lives in `commands::prefetch`.
