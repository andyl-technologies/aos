//! Arguments for `aos build` — build a package (locally or remotely).
//!
//! Build args are inline in `Commands::Build` — no separate struct
//! needed. The implementation lives in `commands::build`.
