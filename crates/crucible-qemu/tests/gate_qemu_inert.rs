//! Red placeholder for `gate:qemu-inert`.

#![forbid(unsafe_code)]

#[test]
#[ignore = "T-HARN-21 implements gate:qemu-inert"]
fn gate_qemu_inert_is_mapped() {
    panic!("gate:qemu-inert implementation is pending T-HARN-21");
}
