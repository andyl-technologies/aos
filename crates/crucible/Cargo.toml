[package]
name = "crucible"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []
test-double = ["dep:crucible-sim"]
qemu-backend = []

[dependencies]
crucible-sim = { path = "../crucible-sim", optional = true }
