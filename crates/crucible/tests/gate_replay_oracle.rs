//! Red placeholder for `gate:replay-oracle` under `--features test-double`.

#![forbid(unsafe_code)]

#[test]
#[ignore = "T-HARN-12 implements gate:replay-oracle"]
fn gate_replay_oracle_is_mapped() {
    panic!("gate:replay-oracle implementation is pending T-HARN-12");
}
