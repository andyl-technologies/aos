# Build and boot AOS from source

This is a maintainer and release-integration workflow. The tutorial now lives
in [Build and boot AOS from source](../../maintainers/source-build-quickstart.md).

End users should install the published system image rather than build a private
image with Nix. A public image is not available during the current early
preview.
