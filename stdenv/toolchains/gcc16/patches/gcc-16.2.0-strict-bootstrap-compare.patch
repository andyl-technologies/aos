--- a/Makefile.in
+++ b/Makefile.in
@@ -66033,19 +66033,37 @@
 	rm -f .bad_compare; \
 	echo Comparing stages 2 and 3; \
         sed=`echo stage3 | sed 's,^stage,,;s,.,.,g'`; \
-	files=`find stage3-* -name "*$(objext)" -print | \
-		 sed -n s,^stage$$sed-,,p`; \
+	find stage2-* -name "*$(objext)" -print | \
+		 sed -n s,^stage2-,,p | LC_ALL=C sort > .stage2_compare_files; \
+	find stage3-* -name "*$(objext)" -print | \
+		 sed -n s,^stage$$sed-,,p | LC_ALL=C sort > .stage3_compare_files; \
+	if test ! -s .stage2_compare_files || test ! -s .stage3_compare_files; then \
+	  echo "Bootstrap comparison has an empty object inventory!"; \
+	  exit 1; \
+	fi; \
+	if ! cmp .stage2_compare_files .stage3_compare_files >/dev/null 2>&1; then \
+	  echo "Bootstrap stage object inventories differ!"; \
+	  diff -u .stage2_compare_files .stage3_compare_files || true; \
+	  exit 1; \
+	fi; \
+	files=`cat .stage3_compare_files`; \
 	for file in $${files} ${extra-compare}; do \
 	  f1=$$r/stage2-$$file; f2=$$r/stage3-$$file; \
-	  if test ! -f $$f1; then continue; fi; \
+	  if test ! -f $$f1 || test ! -f $$f2; then \
+	    echo "missing comparison input: $$file" >> .bad_compare; \
+	    continue; \
+	  fi; \
 	  $(do-compare) > /dev/null 2>&1; \
-	  if test $$? -eq 1; then \
+	  compare_status=$$?; \
+	  if test $$compare_status -eq 1; then \
 	    case $$file in \
 	      @compare_exclusions@) \
 	        echo warning: $$file differs ;; \
 	      *) \
 	        echo $$file differs >> .bad_compare ;; \
 	    esac; \
+	  elif test $$compare_status -ne 0; then \
+	    echo "comparator error $$compare_status: $$file" >> .bad_compare; \
 	  fi; \
 	done; \
 	if [ -f .bad_compare ]; then \
