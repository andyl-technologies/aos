diff --git a/gcc/gengtype.c b/gcc/gengtype.c
index 8e13d91..ad44e50 100644
--- a/gcc/gengtype.c
+++ b/gcc/gengtype.c
@@ -5387,6 +5387,14 @@ htab_eq_inputfile (const void *x, const void *y)
 }
 
 
+static void
+gengtype_trace (const char *stage)
+{
+  fprintf (stderr, "gengtype-stage: %s\n", stage);
+  fflush (stderr);
+}
+
+
 int
 main (int argc, char **argv)
 {
@@ -5394,19 +5402,24 @@ main (int argc, char **argv)
   static struct fileloc pos = { NULL, 0 };
   outf_p output_header;
 
+  gengtype_trace ("main");
   /* Mandatory common initializations.  */
   progname = "gengtype";	/* For fatal and messages.  */
   /* Create the hash-table used to hash-cons input files.  */
   input_file_htab =
     htab_create (800, htab_hash_inputfile, htab_eq_inputfile, NULL);
+  gengtype_trace ("input file hash table created");
   /* Initialize our special input files.  */
   this_file = input_file_by_name (__FILE__);
   system_h_file = input_file_by_name ("system.h");
+  gengtype_trace ("special input files initialized");
   /* Set the scalar_is_char union number for predefined scalar types.  */
   scalar_nonchar.u.scalar_is_char = FALSE;
   scalar_char.u.scalar_is_char = TRUE;
+  gengtype_trace ("scalar flags initialized");
 
   parse_program_options (argc, argv);
+  gengtype_trace ("program options parsed");
 
 #if ENABLE_CHECKING
   if (do_debug)
@@ -5449,12 +5463,21 @@ main (int argc, char **argv)
 			    create_pointer (resolve_typedef ("void", &pos)),
 			    &pos));
 #undef POS_HERE
+      gengtype_trace ("predefined scalar types initialized");
+      gengtype_trace ("reading input list");
       read_input_list (inputlist);
+      gengtype_trace ("input list read");
       for (i = 0; i < num_gt_files; i++)
 	{
+	  fprintf (stderr, "gengtype-stage: parsing file %lu/%lu %s\n",
+		   (unsigned long) i + 1, (unsigned long) num_gt_files,
+		   get_input_file_name (gt_files[i]));
+	  fflush (stderr);
 	  parse_file (get_input_file_name (gt_files[i]));
+	  gengtype_trace ("file parsed");
 	  DBGPRINTF ("parsed file #%d %s", 
 		     (int) i, get_input_file_name (gt_files[i]));
 	}
+      gengtype_trace ("all input files parsed");
       if (verbosity_level >= 1)
 	printf ("%s parsed %d files with %d GTY types\n", 
 		progname, (int) num_gt_files, type_count);
@@ -5507,11 +5530,15 @@ main (int argc, char **argv)
   if (hit_error)
     return 1;
 
+  gengtype_trace ("generating RTX links");
   gen_rtx_next ();
+  gengtype_trace ("RTX links generated");
 
   /* The call to set_gc_used may indirectly call find_param_structure
      hence enlarge the param_structs list of types.  */
+  gengtype_trace ("marking GC-used types");
   set_gc_used (variables);
+  gengtype_trace ("GC-used types marked");
 
  /* The state at this point is read from the state input file or by
     parsing source files and optionally augmented by parsing plugin
@@ -5524,7 +5551,9 @@ main (int argc, char **argv)
 	fatal ("didn't write state file %s after errors", 
 	       write_state_filename);
 
+      gengtype_trace ("writing state");
       DBGPRINTF ("before write_state %s", write_state_filename);
       write_state (write_state_filename);
+      gengtype_trace ("state written");
 
       if (do_dump)
 	dump_everything ();
