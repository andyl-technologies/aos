diff --git a/accel/tcg/tcg-accel-ops-rr.c b/accel/tcg/tcg-accel-ops-rr.c
index 54ce83c22..8043aa3fe 100644
--- a/accel/tcg/tcg-accel-ops-rr.c
+++ b/accel/tcg/tcg-accel-ops-rr.c
@@ -416,6 +416,17 @@ static void rr_crucible_sim_park_vmstop(void)
          * converts those stop requests into CPUState::stopped before we park.
          */
         rr_crucible_sim_drain_vcpu_work();
+        if (qemu_plugin_crucible_vmstop_resume_consume()) {
+            /*
+             * Complete the plugin stop fence on its RR owner before firing
+             * the resume callback.  White-box guest writes are restricted to
+             * this serialized callback, and consuming the fence first lets
+             * the callback admit another exact-boundary stop if necessary.
+             */
+            qemu_plugin_maybe_fire_vcpu_resume_cb(
+                current_cpu ? current_cpu : first_cpu);
+            continue;
+        }
         /*
          * Close the lost-kick window between draining vCPU work and sleeping.
          * The main loop may set CPUState::stop or queue vCPU work and signal
diff --git a/include/qemu/plugin.h b/include/qemu/plugin.h
index fb4ed942d..2c395a2e3 100644
--- a/include/qemu/plugin.h
+++ b/include/qemu/plugin.h
@@ -206,6 +206,7 @@ bool qemu_plugin_crucible_vmstop_admission_pending(void);
 void qemu_plugin_crucible_vmstop_request_stopped(int flush_status);
 int qemu_plugin_crucible_vmstop_flush_status(void);
 void qemu_plugin_crucible_vmstop_request_complete(void);
+bool qemu_plugin_crucible_vmstop_resume_consume(void);
 int qemu_plugin_crucible_lifecycle_complete_terminal_exit(
     const char *action_sha256, const char *evidence_sha256,
     uint64_t process_generation, bool *already_authorized,
@@ -400,6 +401,11 @@ static inline int qemu_plugin_crucible_vmstop_flush_status(void)
 static inline void qemu_plugin_crucible_vmstop_request_complete(void)
 { }
 
+static inline bool qemu_plugin_crucible_vmstop_resume_consume(void)
+{
+    return false;
+}
+
 static inline int qemu_plugin_crucible_lifecycle_complete_terminal_exit(
     const char *action_sha256, const char *evidence_sha256,
     uint64_t process_generation, bool *already_authorized,
diff --git a/include/qemu/qemu-plugin.h b/include/qemu/qemu-plugin.h
index 503378e45..adf967dca 100644
--- a/include/qemu/qemu-plugin.h
+++ b/include/qemu/qemu-plugin.h
@@ -1079,6 +1079,21 @@ int qemu_plugin_icount_at_tb_entry(uint64_t tb_insns,
 QEMU_PLUGIN_API
 void qemu_plugin_force_vcpu_exit(void);
 
+/**
+ * qemu_plugin_crucible_force_vcpu_tb_exit() - exit before the next chained TB
+ *
+ * Requests an exit from the current translated execution batch and sets the
+ * TCG decrementer's high half so the prologue of a directly chained TB returns
+ * to cpu_exec before executing a guest instruction. This operation is valid
+ * only from a running vCPU callback in precise-icount, single-threaded
+ * Crucible sim RR mode.
+ *
+ * Returns zero after requesting the exit, or -EINVAL outside the required
+ * execution context.
+ */
+QEMU_PLUGIN_API
+int qemu_plugin_crucible_force_vcpu_tb_exit(void);
+
 /**
  * qemu_plugin_request_vmstop() - stop the VM from an exact plugin boundary
  *
diff --git a/plugins/api-system.c b/plugins/api-system.c
index 4a1632481..f26eaa1f1 100644
--- a/plugins/api-system.c
+++ b/plugins/api-system.c
@@ -1028,6 +1028,18 @@ void qemu_plugin_force_vcpu_exit(void)
     }
 }
 
+int qemu_plugin_crucible_force_vcpu_tb_exit(void)
+{
+    if (!current_cpu || !current_cpu->running ||
+        icount_enabled() != ICOUNT_PRECISE ||
+        !qemu_plugin_crucible_single_threaded_rr()) {
+        return -EINVAL;
+    }
+
+    cpu_exit(current_cpu);
+    return 0;
+}
+
 static __thread unsigned int qemu_plugin_crucible_exact_boundary_depth;
 static __thread unsigned int qemu_plugin_crucible_control_boundary_depth;
 static int qemu_plugin_crucible_vmstop_state;
@@ -1037,6 +1049,7 @@ enum {
     QEMU_PLUGIN_CRUCIBLE_VMSTOP_IDLE,
     QEMU_PLUGIN_CRUCIBLE_VMSTOP_ADMITTED,
     QEMU_PLUGIN_CRUCIBLE_VMSTOP_STOPPED,
+    QEMU_PLUGIN_CRUCIBLE_VMSTOP_RESUME_PENDING,
 };
 
 static int qemu_plugin_crucible_vmstop_begin(void)
@@ -1168,12 +1181,20 @@ void qemu_plugin_crucible_vmstop_request_complete(void)
 {
     if (qatomic_cmpxchg(&qemu_plugin_crucible_vmstop_state,
                        QEMU_PLUGIN_CRUCIBLE_VMSTOP_STOPPED,
-                       QEMU_PLUGIN_CRUCIBLE_VMSTOP_IDLE) ==
+                       QEMU_PLUGIN_CRUCIBLE_VMSTOP_RESUME_PENDING) ==
         QEMU_PLUGIN_CRUCIBLE_VMSTOP_STOPPED) {
         qatomic_set(&qemu_plugin_crucible_vmstop_flush_result, 0);
     }
 }
 
+bool qemu_plugin_crucible_vmstop_resume_consume(void)
+{
+    return qatomic_cmpxchg(&qemu_plugin_crucible_vmstop_state,
+                           QEMU_PLUGIN_CRUCIBLE_VMSTOP_RESUME_PENDING,
+                           QEMU_PLUGIN_CRUCIBLE_VMSTOP_IDLE) ==
+           QEMU_PLUGIN_CRUCIBLE_VMSTOP_RESUME_PENDING;
+}
+
 int qemu_plugin_crucible_single_threaded_rr(void)
 {
     return strcmp(current_accel_name(), "sim") == 0 &&
diff --git a/target/arm/tcg/translate-a64.c b/target/arm/tcg/translate-a64.c
index 151298e35..418edd8d4 100644
--- a/target/arm/tcg/translate-a64.c
+++ b/target/arm/tcg/translate-a64.c
@@ -1916,6 +1916,16 @@ static bool trans_ERETA(DisasContext *s, arg_reta *a)
 
 static bool trans_NOP(DisasContext *s, arg_NOP *a)
 {
+    /*
+     * Crucible reserves HINT #0x4c as its AArch64 white-box doorbell.  Keep
+     * the architectural NOP semantics, but end this TB after the trap so a
+     * plugin callback can request a deterministic stop before the following
+     * guest instruction.  x86 OUT already has this boundary through
+     * translator_io_start().
+     */
+    if (s->insn == 0xd503299f) {
+        translator_io_start(&s->base);
+    }
     return true;
 }
 
