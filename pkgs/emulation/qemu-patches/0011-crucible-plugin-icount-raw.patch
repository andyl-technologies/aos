diff --git a/include/qemu/qemu-plugin.h b/include/qemu/qemu-plugin.h
--- a/include/qemu/qemu-plugin.h
+++ b/include/qemu/qemu-plugin.h
@@ -762,6 +762,16 @@ void qemu_plugin_advance_virtual_time_direct(int64_t time);
 QEMU_PLUGIN_API
 void qemu_plugin_drain_main_loop(void);
 
+/**
+ * qemu_plugin_icount_raw() - return raw retired-instruction count
+ *
+ * Returns QEMU's raw icount value without the virtual-clock bias used by
+ * qemu_plugin_update_ns() and the ns conversion path. Crucible uses this as the
+ * fingerprint and divergence-bisection instruction axis.
+ */
+QEMU_PLUGIN_API
+uint64_t qemu_plugin_icount_raw(void);
+
 /**
  * qemu_plugin_net_inject() - inject an inbound frame into the default NIC
  * @data: Ethernet frame bytes
diff --git a/plugins/api-system.c b/plugins/api-system.c
--- a/plugins/api-system.c
+++ b/plugins/api-system.c
@@ -19,6 +19,7 @@
 #include "qemu/timer.h"
 #include "qemu/plugin-memory.h"
 #include "qemu/plugin.h"
+#include "system/cpu-timers.h"
 
 /*
  * In system mode we cannot trace the binary being executed so the
@@ -176,6 +177,21 @@ void qemu_plugin_drain_main_loop(void)
     qemu_plugin_drain_bottom_halves();
 }
 
+uint64_t qemu_plugin_icount_raw(void)
+{
+    int64_t raw_icount;
+
+    if (!icount_enabled()) {
+        return 0;
+    }
+
+    raw_icount = icount_get_raw();
+    if (raw_icount < 0) {
+        return 0;
+    }
+    return (uint64_t)raw_icount;
+}
+
 
 static NetClientState *qemu_plugin_default_nic_queue(void)
 {
