diff --git a/accel/tcg/icount-common.c b/accel/tcg/icount-common.c
index f52cc2c26..883996e5e 100644
--- a/accel/tcg/icount-common.c
+++ b/accel/tcg/icount-common.c
@@ -30,6 +30,8 @@
 #include "qemu/error-report.h"
 #include "system/cpus.h"
 #include "system/qtest.h"
+#include "qemu/accel.h"
+#include "qemu/plugin.h"
 #include "qemu/main-loop.h"
 #include "qemu/option.h"
 #include "qemu/seqlock.h"
@@ -357,6 +359,17 @@ void icount_start_warp_timer(void)
         }
     }
 
+    if (strcmp(current_accel_name(), "sim") == 0 &&
+        qemu_plugin_has_time_control()) {
+        /*
+         * In sim mode, a time-control plugin owns idle advancement. Keep the
+         * virtual-clock notification that wakes the main loop, but do not
+         * advance qemu_icount_bias or arm the realtime warp timer.
+         */
+        qemu_clock_notify(QEMU_CLOCK_VIRTUAL);
+        return;
+    }
+
     /* We want to use the earliest deadline from ALL vm_clocks */
     clock = qemu_clock_get_ns(QEMU_CLOCK_VIRTUAL_RT);
     deadline = qemu_clock_deadline_ns_all(QEMU_CLOCK_VIRTUAL,
diff --git a/include/qemu/plugin.h b/include/qemu/plugin.h
index 9c2ec8cea..5d9118845 100644
--- a/include/qemu/plugin.h
+++ b/include/qemu/plugin.h
@@ -183,6 +183,8 @@ void qemu_plugin_flush_cb(void);
 
 void qemu_plugin_atexit_cb(void);
 
+bool qemu_plugin_has_time_control(void);
+
 void qemu_plugin_add_dyn_cb_arr(GArray *arr);
 
 static inline void qemu_plugin_disable_mem_helpers(CPUState *cpu)
@@ -237,6 +239,11 @@ static inline enum qemu_plugin_cb_flags qemu_plugin_get_cb_flags(void)
 static inline void qemu_plugin_add_opts(void)
 { }
 
+static inline bool qemu_plugin_has_time_control(void)
+{
+    return false;
+}
+
 static inline void qemu_plugin_opt_parse(const char *optstr,
                                          QemuPluginList *head)
 {
diff --git a/plugins/api-system.c b/plugins/api-system.c
index fbf905cd6..ffecb669f 100644
--- a/plugins/api-system.c
+++ b/plugins/api-system.c
@@ -100,7 +100,11 @@ const char *qemu_plugin_hwaddr_device_name(const struct qemu_plugin_hwaddr *h)
  * Time control
  */
 static bool has_control;
-static Error *migration_blocker;
+
+bool qemu_plugin_has_time_control(void)
+{
+    return has_control;
+}
 
 const void *qemu_plugin_request_time_control(void)
 {
