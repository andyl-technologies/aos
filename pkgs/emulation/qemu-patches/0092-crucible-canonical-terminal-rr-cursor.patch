diff --git a/include/qemu/qemu-plugin.h b/include/qemu/qemu-plugin.h
index 1d52484d1..65df26295 100644
--- a/include/qemu/qemu-plugin.h
+++ b/include/qemu/qemu-plugin.h
@@ -2163,7 +2163,10 @@ struct qemu_plugin_rr_cursor {
  * RR owner or from an exact deterministic plugin boundary, where QEMU reads
  * the authoritative committed cursor even if no vCPU is current. At exact
  * raw-zero genesis before the first selection, it reports the canonical next
- * coordinate (vCPU 0, position 0) without mutating scheduler state. It returns
+ * coordinate (vCPU 0, position 0) without mutating scheduler state. A live
+ * observer at the terminal position of a quantum reports the canonical next
+ * coordinate (the scheduler's next vCPU, position 0), also without mutating
+ * scheduler state. It returns
  * nonzero in every other unowned context, if no pinned rr_switch_quantum is
  * active, or if the cursor is not inside that quantum. The read is
  * side-effect-free with respect to guest state.
diff --git a/plugins/api.c b/plugins/api.c
index efce95bb0..186d36655 100644
--- a/plugins/api.c
+++ b/plugins/api.c
@@ -694,6 +694,7 @@ int qemu_plugin_read_vcpu_regs(unsigned int vcpu_index,
 int qemu_plugin_rr_cursor(struct qemu_plugin_rr_cursor *out)
 {
     CPUState *cpu = current_cpu;
+    CPUState *next_cpu;
     bool exact_boundary = qemu_plugin_crucible_exact_boundary_active();
     uint64_t current_vcpu = icount_crucible_rr_current_vcpu();
     uint64_t cursor_position = exact_boundary ?
@@ -703,6 +704,21 @@ int qemu_plugin_rr_cursor(struct qemu_plugin_rr_cursor *out)
     int num_vcpus = qemu_plugin_num_vcpus();
     bool genesis_cursor = exact_boundary && current_vcpu == UINT64_MAX &&
         icount_get_raw_observed() == 0 && cursor_position == 0;
+    bool terminal_cursor = !exact_boundary && cpu &&
+        current_vcpu == (uint64_t)cpu->cpu_index &&
+        rr_switch_quantum != 0 && cursor_position == rr_switch_quantum;
+
+    if (terminal_cursor) {
+        next_cpu = CPU_NEXT(cpu);
+        if (!next_cpu) {
+            next_cpu = first_cpu;
+        }
+        if (!next_cpu) {
+            return -1;
+        }
+        current_vcpu = next_cpu->cpu_index;
+        cursor_position = 0;
+    }
 
     if (!out || (!cpu && !exact_boundary) ||
         (current_vcpu == UINT64_MAX && !genesis_cursor) ||
