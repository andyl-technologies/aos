diff --git a/src/nspawn/nspawn-root-fd.c b/src/nspawn/nspawn-root-fd.c
index 2db084a..884229b 100644
--- a/src/nspawn/nspawn-root-fd.c
+++ b/src/nspawn/nspawn-root-fd.c
@@ -2,6 +2,8 @@
 
 #include <fcntl.h>
 #include <sys/file.h>
+#include <sys/mount.h>
+#include <sys/socket.h>
 #include <sys/stat.h>
 #include <unistd.h>
 
@@ -9,7 +11,11 @@
 
 #include "alloc-util.h"
 #include "fd-util.h"
+#include "namespace-util.h"
 #include "nspawn-root-fd.h"
+#include "pidref.h"
+#include "process-util.h"
+#include "socket-util.h"
 #include "string-util.h"
 #include "strv.h"
 
@@ -55,9 +61,132 @@ int aos_root_directory_from_fd(int fd, int *ret_root_fd, int *ret_lock_fd, char
         return 0;
 }
 
-int aos_root_directory_acquire(int *ret_root_fd, int *ret_lock_fd, char **ret_path) {
+int aos_attachment_anchor_from_fd(int fd, int *ret_anchor_fd) {
+        _cleanup_close_ int anchor_fd = -EBADF;
+        struct stat source;
+
+        assert(ret_anchor_fd);
+
+        anchor_fd = fcntl(fd, F_DUPFD_CLOEXEC, SD_LISTEN_FDS_START);
+        if (anchor_fd < 0)
+                return -errno;
+        if (fstat(anchor_fd, &source) < 0)
+                return -errno;
+        if (!S_ISDIR(source.st_mode))
+                return -ENOTDIR;
+        if (source.st_uid != geteuid() || (source.st_mode & 07777) != 0755)
+                return -EPERM;
+
+        *ret_anchor_fd = TAKE_FD(anchor_fd);
+        return 0;
+}
+
+int aos_attachment_anchor_namespace_from_fd(int fd, int *ret_namespace_fd) {
+        _cleanup_close_ int namespace_fd = -EBADF;
+        int r;
+
+        assert(ret_namespace_fd);
+
+        namespace_fd = fcntl(fd, F_DUPFD_CLOEXEC, SD_LISTEN_FDS_START);
+        if (namespace_fd < 0)
+                return -errno;
+        r = fd_is_namespace(namespace_fd, NAMESPACE_MOUNT);
+        if (r < 0)
+                return r;
+        if (r == 0)
+                return -EINVAL;
+
+        *ret_namespace_fd = TAKE_FD(namespace_fd);
+        return 0;
+}
+
+int aos_attachment_anchor_clone(
+                int anchor_fd,
+                int source_namespace_fd,
+                int *ret_mount_fd) {
+
+        _cleanup_close_pair_ int errno_pipe[2] = EBADF_PAIR, transfer_socket[2] = EBADF_PAIR;
+        _cleanup_(pidref_done) PidRef child = PIDREF_NULL;
+        int r;
+
+        assert(anchor_fd >= 0);
+        assert(source_namespace_fd >= 0);
+        assert(ret_mount_fd);
+
+        r = fd_is_namespace(source_namespace_fd, NAMESPACE_MOUNT);
+        if (r < 0)
+                return r;
+        if (r == 0)
+                return -EINVAL;
+
+        if (pipe2(errno_pipe, O_CLOEXEC) < 0)
+                return -errno;
+        if (socketpair(AF_UNIX, SOCK_DGRAM|SOCK_CLOEXEC, 0, transfer_socket) < 0)
+                return -errno;
+
+        /* The attached anchor is meaningful only in the Host service's mount
+         * namespace. Enter it only in a short-lived child, clone the exact
+         * validated descriptor, and return a detached mount to the parent. */
+        r = namespace_fork(
+                        "(aos-anchor-ns)",
+                        "(aos-anchor-clone)",
+                        FORK_RESET_SIGNALS|FORK_DEATHSIG_SIGKILL,
+                        /* pidns_fd= */ -EBADF,
+                        source_namespace_fd,
+                        /* netns_fd= */ -EBADF,
+                        /* userns_fd= */ -EBADF,
+                        /* root_fd= */ -EBADF,
+                        &child);
+        if (r < 0)
+                return r;
+        if (r == 0) {
+                _cleanup_close_ int mount_fd = -EBADF;
+
+                errno_pipe[0] = safe_close(errno_pipe[0]);
+                transfer_socket[0] = safe_close(transfer_socket[0]);
+
+                mount_fd = open_tree(
+                                anchor_fd,
+                                "",
+                                OPEN_TREE_CLONE|OPEN_TREE_CLOEXEC|AT_EMPTY_PATH|AT_RECURSIVE);
+                if (mount_fd < 0)
+                        report_errno_and_exit(errno_pipe[1], -errno);
+
+                r = send_one_fd(transfer_socket[1], mount_fd, /* flags= */ 0);
+                if (r < 0)
+                        report_errno_and_exit(errno_pipe[1], r);
+
+                _exit(EXIT_SUCCESS);
+        }
+
+        errno_pipe[1] = safe_close(errno_pipe[1]);
+        transfer_socket[1] = safe_close(transfer_socket[1]);
+
+        r = pidref_wait_for_terminate_and_check("(aos-anchor-ns)", &child, /* flags= */ 0);
+        if (r < 0)
+                return r;
+
+        r = read_errno(errno_pipe[0]);
+        if (r < 0)
+                return r;
+
+        r = receive_one_fd(transfer_socket[0], MSG_DONTWAIT);
+        if (r < 0)
+                return r;
+
+        *ret_mount_fd = r;
+        return 0;
+}
+
+int aos_root_directory_acquire(
+                bool with_attachment_anchor,
+                int *ret_root_fd,
+                int *ret_lock_fd,
+                char **ret_path,
+                int *ret_anchor_fd,
+                int *ret_anchor_namespace_fd) {
         _cleanup_strv_free_ char **names = NULL;
-        int n, r;
+        int expected, n, r;
 
         /* This is a setup-only descriptor, never a payload activation socket.
          * Unsetting LISTEN_* makes the later fdset collector empty. Require the
@@ -66,10 +195,22 @@ int aos_root_directory_acquire(int *ret_root_fd, int *ret_lock_fd, char **ret_pa
         n = sd_listen_fds_with_names(/* unset_environment= */ true, &names);
         if (n < 0)
                 return n;
-        if (n != 1 || !streq_ptr(names[0], AOS_ROOT_DIRECTORY_FD_NAME))
+        expected = with_attachment_anchor ? 3 : 1;
+        if (n != expected ||
+            !streq_ptr(names[0], AOS_ROOT_DIRECTORY_FD_NAME) ||
+            (with_attachment_anchor &&
+             (!streq_ptr(names[1], AOS_ATTACHMENT_ANCHOR_FD_NAME) ||
+              !streq_ptr(names[2], AOS_ATTACHMENT_ANCHOR_NAMESPACE_FD_NAME))))
                 r = -EINVAL;
-        else
+        else {
                 r = aos_root_directory_from_fd(SD_LISTEN_FDS_START, ret_root_fd, ret_lock_fd, ret_path);
+                if (r >= 0 && with_attachment_anchor)
+                        r = aos_attachment_anchor_from_fd(SD_LISTEN_FDS_START + 1, ret_anchor_fd);
+                if (r >= 0 && with_attachment_anchor)
+                        r = aos_attachment_anchor_namespace_from_fd(
+                                        SD_LISTEN_FDS_START + 2,
+                                        ret_anchor_namespace_fd);
+        }
 
         for (int i = 0; i < n; i++)
                 safe_close(SD_LISTEN_FDS_START + i);
diff --git a/src/nspawn/nspawn-root-fd.h b/src/nspawn/nspawn-root-fd.h
index 1356eea..cc356ab 100644
--- a/src/nspawn/nspawn-root-fd.h
+++ b/src/nspawn/nspawn-root-fd.h
@@ -2,6 +2,17 @@
 #pragma once
 
 #define AOS_ROOT_DIRECTORY_FD_NAME "aos-sandbox-root-mount-v1"
+#define AOS_ATTACHMENT_ANCHOR_FD_NAME "aos-sandbox-attachment-anchor-v1"
+#define AOS_ATTACHMENT_ANCHOR_NAMESPACE_FD_NAME "aos-sandbox-attachment-anchor-namespace-v1"
 
 int aos_root_directory_from_fd(int fd, int *ret_root_fd, int *ret_lock_fd, char **ret_path);
-int aos_root_directory_acquire(int *ret_root_fd, int *ret_lock_fd, char **ret_path);
+int aos_attachment_anchor_from_fd(int fd, int *ret_anchor_fd);
+int aos_attachment_anchor_namespace_from_fd(int fd, int *ret_namespace_fd);
+int aos_attachment_anchor_clone(int anchor_fd, int source_namespace_fd, int *ret_mount_fd);
+int aos_root_directory_acquire(
+                bool with_attachment_anchor,
+                int *ret_root_fd,
+                int *ret_lock_fd,
+                char **ret_path,
+                int *ret_anchor_fd,
+                int *ret_anchor_namespace_fd);
diff --git a/src/nspawn/nspawn.c b/src/nspawn/nspawn.c
index 366403b..e0c84b2 100644
--- a/src/nspawn/nspawn.c
+++ b/src/nspawn/nspawn.c
@@ -149,6 +149,7 @@ typedef enum ContainerStatus {
 
 static char *arg_directory = NULL;
 static bool arg_aos_root_directory_fd = false;
+static bool arg_aos_attachment_anchor_fd = false;
 static char *arg_template = NULL;
 static char *arg_chdir = NULL;
 static char *arg_pivot_root_new = NULL;
@@ -1175,6 +1176,17 @@ static int parse_argv(int argc, char *argv[]) {
                         arg_aos_root_directory_fd = true;
                         break;
 
+                OPTION_LONG("aos-attachment-anchor-fd", "NAME",
+                            "Install the named AOS destination anchor read-only"):
+                        if (arg_aos_attachment_anchor_fd ||
+                            !streq(opts.arg, AOS_ATTACHMENT_ANCHOR_FD_NAME))
+                                return log_error_errno(
+                                                SYNTHETIC_ERRNO(EINVAL),
+                                                "Invalid or repeated AOS attachment-anchor descriptor role.");
+
+                        arg_aos_attachment_anchor_fd = true;
+                        break;
+
                 OPTION_LONG("restrict-address-families", "LIST", "Restrict socket address families to the given allowlist"):
                         r = parse_address_families(opts.arg, &arg_restrict_address_families, &arg_restrict_address_families_is_allowlist);
                         if (r < 0)
@@ -3973,10 +3985,76 @@ static DissectImageFlags determine_dissect_image_flags(void) {
                  (arg_userns_ownership != USER_NAMESPACE_OWNERSHIP_AUTO) ? DISSECT_IMAGE_IDENTITY_UID : 0);
 }
 
+static int install_aos_attachment_anchor(
+                const char *directory,
+                int attachment_anchor_fd,
+                int idmap_userns_fd) {
+
+        _cleanup_close_ int target_fd = -EBADF;
+        struct stat target_stat;
+        int r;
+
+        assert(directory);
+        assert(attachment_anchor_fd >= 0);
+        assert(idmap_userns_fd >= 0);
+
+        struct mount_attr attr = {
+                .attr_set = MOUNT_ATTR_IDMAP|MOUNT_ATTR_RDONLY|MOUNT_ATTR_NOSUID|
+                            MOUNT_ATTR_NODEV|MOUNT_ATTR_NOEXEC,
+                .userns_fd = idmap_userns_fd,
+        };
+        if (mount_setattr(attachment_anchor_fd, "", AT_EMPTY_PATH|AT_RECURSIVE, &attr, sizeof(attr)) < 0)
+                return log_error_errno(errno, "Failed to protect AOS attachment anchor: %m");
+
+        r = userns_mkdir(directory, "/run/aos", 0755, 0, 0);
+        if (r < 0)
+                return log_error_errno(r, "Failed to create AOS runtime directory: %m");
+
+        r = userns_mkdir(directory, "/run/aos/attachments", 0555, 0, 0);
+        if (r < 0)
+                return log_error_errno(r, "Failed to create AOS attachment-anchor target: %m");
+
+        target_fd = chase_and_open(
+                        "/run/aos/attachments",
+                        directory,
+                        CHASE_PREFIX_ROOT|CHASE_PROHIBIT_SYMLINKS|CHASE_MUST_BE_DIRECTORY,
+                        O_PATH|O_DIRECTORY|O_CLOEXEC,
+                        NULL);
+        if (target_fd < 0)
+                return log_error_errno(target_fd, "Failed to resolve AOS attachment-anchor target: %m");
+        if (fstat(target_fd, &target_stat) < 0)
+                return log_error_errno(errno, "Failed to inspect AOS attachment-anchor target: %m");
+        if (target_stat.st_uid != arg_uid_shift ||
+            target_stat.st_gid != (gid_t) arg_uid_shift ||
+            (target_stat.st_mode & 07777) != 0555)
+                return log_error_errno(
+                                SYNTHETIC_ERRNO(EPERM),
+                                "AOS attachment-anchor target has unsafe ownership or mode.");
+
+        r = is_mount_point_at(target_fd, /* path= */ NULL, /* flags= */ 0);
+        if (r < 0)
+                return log_error_errno(r, "Failed to inspect AOS attachment-anchor mountpoint: %m");
+        if (r > 0)
+                return log_error_errno(
+                                SYNTHETIC_ERRNO(EBUSY),
+                                "AOS attachment-anchor target is already a mountpoint.");
+
+        if (move_mount(
+                        attachment_anchor_fd,
+                        "",
+                        target_fd,
+                        "",
+                        MOVE_MOUNT_F_EMPTY_PATH|MOVE_MOUNT_T_EMPTY_PATH) < 0)
+                return log_error_errno(errno, "Failed to install AOS attachment anchor: %m");
+
+        return 0;
+}
+
 static int outer_child(
                 Barrier *barrier,
                 const char *directory,
                 int mount_fd,
+                int attachment_anchor_fd,
                 DissectedImage *dissected_image,
                 MStack *mstack,
                 int fd_outer_socket,
@@ -3986,6 +4064,7 @@ static int outer_child(
                 const char *unix_export_path) {
 
         _cleanup_strv_free_ char **os_release_pairs = NULL;
+        _cleanup_close_ int idmap_userns_fd = -EBADF;
         bool idmap = false;
         ssize_t l;
         int r;
@@ -4037,8 +4116,6 @@ static int outer_child(
                 assert(!arg_mstack);
 
                 if (arg_aos_root_directory_fd) {
-                        _cleanup_close_ int idmap_userns_fd = -EBADF;
-
                         /* Attribute only the detached root before attachment.
                          * Upstream's later pathname remap unmounts the root,
                          * which fails with EBUSY for a prepared tree containing
@@ -4485,6 +4562,16 @@ static int outer_child(
                         return r;
         }
 
+        if (attachment_anchor_fd >= 0) {
+                assert(arg_aos_attachment_anchor_fd);
+
+                r = install_aos_attachment_anchor(directory, attachment_anchor_fd, idmap_userns_fd);
+                if (r < 0)
+                        return r;
+
+                attachment_anchor_fd = safe_close(attachment_anchor_fd);
+        }
+
         /* We have different codepaths here for privileged and non-privileged mode. In privileged mode we'll
          * now switch into the target directory, and then do the final setup from there. If a user namespace
          * is then allocated for the container, the root mount and everything else will be out of reach for
@@ -5395,6 +5482,7 @@ static int run_container(
                 const char *runtime_dir,
                 const char *directory,
                 int mount_fd,
+                int attachment_anchor_fd,
                 DissectedImage *dissected_image,
                 MStack *mstack,
                 int userns_fd,
@@ -5548,6 +5636,7 @@ static int run_container(
                 r = outer_child(&barrier,
                                 directory,
                                 mount_fd,
+                                attachment_anchor_fd,
                                 dissected_image,
                                 mstack,
                                 fd_outer_socket_pair[1],
@@ -6272,7 +6361,11 @@ static int do_cleanup(void) {
 static int run(int argc, char *argv[]) {
         bool remove_image = false, veth_created = false;
         _cleanup_close_ int master = -EBADF, userns_fd = -EBADF, mount_fd = -EBADF;
-        _cleanup_close_ int aos_root_fd = -EBADF, aos_root_lock_fd = -EBADF;
+        _cleanup_close_ int
+                aos_root_fd = -EBADF,
+                aos_root_lock_fd = -EBADF,
+                aos_attachment_anchor_fd = -EBADF,
+                aos_attachment_anchor_namespace_fd = -EBADF;
         _cleanup_fdset_free_ FDSet *fds = NULL;
         int r, ret = EXIT_SUCCESS;
         char veth_name[IFNAMSIZ] = "";
@@ -6295,6 +6388,13 @@ static int run(int argc, char *argv[]) {
         if (r <= 0)
                 goto finish;
 
+        if (arg_aos_attachment_anchor_fd && !arg_aos_root_directory_fd) {
+                r = log_error_errno(
+                                SYNTHETIC_ERRNO(EINVAL),
+                                "AOS attachment anchor requires an AOS root descriptor.");
+                goto finish;
+        }
+
         if (arg_aos_root_directory_fd) {
                 /* Descriptor roots have no pathname identity for image lookup,
                  * snapshots, registration, or settings discovery. Keep this
@@ -6312,11 +6412,33 @@ static int run(int argc, char *argv[]) {
                         goto finish;
                 }
 
-                r = aos_root_directory_acquire(&aos_root_fd, &aos_root_lock_fd, &arg_directory);
+                r = aos_root_directory_acquire(
+                                arg_aos_attachment_anchor_fd,
+                                &aos_root_fd,
+                                &aos_root_lock_fd,
+                                &arg_directory,
+                                &aos_attachment_anchor_fd,
+                                &aos_attachment_anchor_namespace_fd);
                 if (r < 0) {
                         log_error_errno(r, "Failed to acquire AOS root descriptor: %m");
                         goto finish;
                 }
+
+                if (arg_aos_attachment_anchor_fd) {
+                        _cleanup_close_ int detached_anchor_fd = -EBADF;
+
+                        r = aos_attachment_anchor_clone(
+                                        aos_attachment_anchor_fd,
+                                        aos_attachment_anchor_namespace_fd,
+                                        &detached_anchor_fd);
+                        if (r < 0) {
+                                log_error_errno(r, "Failed to import AOS attachment anchor: %m");
+                                goto finish;
+                        }
+
+                        close_and_replace(aos_attachment_anchor_fd, detached_anchor_fd);
+                        aos_attachment_anchor_namespace_fd = safe_close(aos_attachment_anchor_namespace_fd);
+                }
         }
 
         if (arg_cleanup)
@@ -6962,10 +7084,24 @@ static int run(int argc, char *argv[]) {
                         break;
                 }
 
+                _cleanup_close_ int aos_attachment_anchor_mount_fd = -EBADF;
+
+                if (arg_aos_attachment_anchor_fd) {
+                        aos_attachment_anchor_mount_fd = mount_fd_clone(
+                                        aos_attachment_anchor_fd,
+                                        /* recursive= */ true,
+                                        &aos_attachment_anchor_fd);
+                        if (aos_attachment_anchor_mount_fd < 0) {
+                                r = log_error_errno(aos_attachment_anchor_mount_fd, "Failed to clone AOS attachment anchor: %m");
+                                goto finish;
+                        }
+                }
+
                 r = run_container(
                                 runtime_dir,
                                 rootdir,
                                 arg_aos_root_directory_fd ? aos_mount_fd : mount_fd,
+                                aos_attachment_anchor_mount_fd,
                                 dissected_image,
                                 mstack,
                                 userns_fd,
diff --git a/src/nspawn/test-nspawn-root-fd.c b/src/nspawn/test-nspawn-root-fd.c
index 5c1f795..05b9aa3 100644
--- a/src/nspawn/test-nspawn-root-fd.c
+++ b/src/nspawn/test-nspawn-root-fd.c
@@ -57,40 +57,84 @@ TEST(rejects_host_root_and_non_directory) {
         ASSERT_ERROR(aos_root_directory_from_fd(-1, &root, &lock, &alias), EBADF);
 }
 
+TEST(attachment_anchor_requires_a_protected_directory) {
+        _cleanup_(rm_rf_physical_and_freep) char *directory = NULL;
+        _cleanup_close_ int source = -EBADF, anchor = -EBADF;
+
+        ASSERT_OK(mkdtemp_malloc("/tmp/test-nspawn-anchor-fd.XXXXXX", &directory));
+        ASSERT_OK_ERRNO(chmod(directory, 0755));
+        ASSERT_OK_ERRNO(source = open(directory, O_PATH|O_DIRECTORY|O_CLOEXEC));
+        ASSERT_OK(aos_attachment_anchor_from_fd(source, &anchor));
+        ASSERT_TRUE(FLAGS_SET(fcntl(anchor, F_GETFD), FD_CLOEXEC));
+
+        anchor = safe_close(anchor);
+        ASSERT_OK_ERRNO(chmod(directory, 0500));
+        ASSERT_ERROR(aos_attachment_anchor_from_fd(source, &anchor), EPERM);
+}
+
 TEST(setup_role_is_exact_and_not_forwarded) {
         _cleanup_(rm_rf_physical_and_freep) char *directory = NULL;
+        _cleanup_free_ char *anchor_directory = NULL;
         static const struct {
+                bool with_anchor;
                 const char *count;
                 const char *names;
                 int expected;
         } cases[] = {
-                { "1", AOS_ROOT_DIRECTORY_FD_NAME, 0 },
-                { "0", "", -EINVAL },
-                { "1", "unexpected", -EINVAL },
-                { "2", AOS_ROOT_DIRECTORY_FD_NAME ":unexpected", -EINVAL },
+                { false, "1", AOS_ROOT_DIRECTORY_FD_NAME, 0 },
+                { true,
+                  "3",
+                  AOS_ROOT_DIRECTORY_FD_NAME ":" AOS_ATTACHMENT_ANCHOR_FD_NAME ":" AOS_ATTACHMENT_ANCHOR_NAMESPACE_FD_NAME,
+                  0 },
+                { false, "0", "", -EINVAL },
+                { false, "1", "unexpected", -EINVAL },
+                { true, "3", AOS_ROOT_DIRECTORY_FD_NAME ":unexpected:unexpected", -EINVAL },
         };
 
         ASSERT_OK(mkdtemp_malloc("/tmp/test-nspawn-root-role.XXXXXX", &directory));
+        ASSERT_NOT_NULL(anchor_directory = path_join(directory, "anchor"));
+        ASSERT_OK_ERRNO(mkdir(anchor_directory, 0755));
         FOREACH_ELEMENT(c, cases) {
                 int r;
 
                 ASSERT_OK(r = pidref_safe_fork("(aos-root-role)", FORK_WAIT, NULL));
                 if (r == 0) {
                         _cleanup_free_ char *pid = NULL, *alias = NULL;
-                        int source, root = -EBADF, lock = -EBADF;
+                        int source, anchor_source, namespace_source;
+                        int root = -EBADF, lock = -EBADF, anchor = -EBADF, anchor_namespace = -EBADF;
 
                         ASSERT_OK_ERRNO(source = open(directory, O_PATH|O_DIRECTORY|O_CLOEXEC));
                         ASSERT_OK_ERRNO(dup2(source, SD_LISTEN_FDS_START));
                         if (source != SD_LISTEN_FDS_START)
                                 safe_close(source);
-                        if (c->count[0] == '2')
-                                ASSERT_OK_ERRNO(dup2(SD_LISTEN_FDS_START, SD_LISTEN_FDS_START + 1));
+                        if (c->count[0] == '3') {
+                                ASSERT_OK_ERRNO(anchor_source = open(
+                                                anchor_directory,
+                                                O_PATH|O_DIRECTORY|O_CLOEXEC));
+                                ASSERT_OK_ERRNO(dup2(anchor_source, SD_LISTEN_FDS_START + 1));
+                                if (anchor_source != SD_LISTEN_FDS_START + 1)
+                                        safe_close(anchor_source);
+                                ASSERT_OK_ERRNO(namespace_source = open(
+                                                "/proc/self/ns/mnt",
+                                                O_RDONLY|O_CLOEXEC));
+                                ASSERT_OK_ERRNO(dup2(namespace_source, SD_LISTEN_FDS_START + 2));
+                                if (namespace_source != SD_LISTEN_FDS_START + 2)
+                                        safe_close(namespace_source);
+                        }
                         ASSERT_OK_ERRNO(asprintf(&pid, "%d", getpid()));
                         ASSERT_OK_ERRNO(setenv("LISTEN_PID", pid, 1));
                         ASSERT_OK_ERRNO(setenv("LISTEN_FDS", c->count, 1));
                         ASSERT_OK_ERRNO(setenv("LISTEN_FDNAMES", c->names, 1));
 
-                        ASSERT_EQ(aos_root_directory_acquire(&root, &lock, &alias), c->expected);
+                        ASSERT_EQ(
+                                        aos_root_directory_acquire(
+                                                        c->with_anchor,
+                                                        &root,
+                                                        &lock,
+                                                        &alias,
+                                                        &anchor,
+                                                        &anchor_namespace),
+                                        c->expected);
                         ASSERT_NULL(getenv("LISTEN_PID"));
                         ASSERT_NULL(getenv("LISTEN_FDS"));
                         ASSERT_NULL(getenv("LISTEN_FDNAMES"));
