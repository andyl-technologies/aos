These ten Java files come from Apache Tomcat 6.0.53's
`java/javax/annotation/` directory. `LICENSE` and `NOTICE` are copied from the
same release.

Upstream source archive:
`https://archive.apache.org/dist/tomcat/tomcat-6/v6.0.53/src/apache-tomcat-6.0.53-src.tar.gz`

Archive SHA-256:
`sha256-A5Kh0EVUyRuO/DxivsvaY03iAx8DZpJTeARwYqIJjTA=`

The upstream archive also contains precompiled example JARs. Only the ten
UTF-8 Java files and license texts listed above were extracted into this
directory. Trailing whitespace was removed from the selected text files. The
AOS build never fetches or retains the compiled files in the upstream archive.
